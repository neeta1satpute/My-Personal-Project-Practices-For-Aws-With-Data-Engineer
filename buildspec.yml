version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.9
    commands:
      - echo "Installing dependencies..."
      - python --version
      - python -m pip install --upgrade pip


  build:
    commands:
      - echo "Running tests..."
      - python -m pytest tests/ || true

artifacts:
  files:
    - '**/*'
