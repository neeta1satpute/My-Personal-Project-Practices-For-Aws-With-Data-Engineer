version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.9
    commands:
      - echo "Installing dependencies..."
      - python --version
  build:
    commands:
      - echo "Running tests..."
artifacts:
  files:
    - '**/*'
